<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.herry.imagecompress.MainActivity">

    <Button
        android:id="@+id/choose_image_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose Photo" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerview_origin"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/choose_image_btn"
        android:layout_marginTop="10dp"
        android:background="#0f434343"></android.support.v7.widget.RecyclerView>

    <Button
        android:id="@+id/compress_image_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/recyclerview_origin"
        android:layout_marginTop="10dp"
        android:text="Compress Selected Photo" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerview_compressed"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/compress_image_btn"
        android:layout_marginTop="10dp"
        android:background="#0f434343"></android.support.v7.widget.RecyclerView>
</RelativeLayout>
